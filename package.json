{
  "name": "request_multiple_urls",
  "version": "1.1.0",
  "description": "it is a simple function which is going to fetch an array of urls and return the data",
  "main": "index.js",
  "repository": "AmirTGitHub/request_multiple_urls",
  "scripts": {
    "test": "mocha  --timeout 2000 ./test/index.test.js"
  },
  "keywords": [],
  "author": "Amir Gevari",
  "license": "ISC",
  "dependencies": {
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.0.0"
  }
}
